// the data that can be stored as value in bucket(slot in memory)

// assing
let x = 10;
x = x + 2; /* assigning new value to 2 to the existing value of x and
            then store in the same variable */

var y = 20;
const z = "abckd";
console.log(x, y, z);
